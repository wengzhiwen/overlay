import type { FrameSnapshot } from "../../domain/frame-data.js";

export type DistanceWidgetProps = {
  frame: FrameSnapshot;
};

export const DistanceWidget = (_props: DistanceWidgetProps): null => {
  return null;
};
