import type { FrameSnapshot } from "../../domain/frame-data.js";

export type SpeedWidgetProps = {
  frame: FrameSnapshot;
};

export const SpeedWidget = (_props: SpeedWidgetProps): null => {
  return null;
};
