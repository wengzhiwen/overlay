import type { FrameSnapshot } from "../../domain/frame-data.js";

export type ElevationWidgetProps = {
  frame: FrameSnapshot;
};

export const ElevationWidget = (_props: ElevationWidgetProps): null => {
  return null;
};
