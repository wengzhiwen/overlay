import type { FrameSnapshot } from "../../domain/frame-data.js";

export type HeartRateWidgetProps = {
  frame: FrameSnapshot;
};

export const HeartRateWidget = (_props: HeartRateWidgetProps): null => {
  return null;
};
